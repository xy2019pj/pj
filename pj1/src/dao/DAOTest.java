package dao;
class DAOTest {
	DAO dao = new DAO();
	/*
        @Test
        void testUpdate() {
            String sql = "insert into costomers(name,email,birth) values(?,?,?)";
            dao.update(sql, "ะกร๗","xiaoming@123.com",new Date(new java.util.Date().getTime()));
        }

        @Test
        void testGet() {
            String sql = "SELECT id thisId,name,email,birth FROM costomers Where id = ?";
            Customer customer = dao.get(Customer.class, sql, 5);

            System.out.println(customer.getName());
        }

        @Test
        void testUpdate() {
            String sql = "insert into students(name,grade) values(?,?)";
            dao.update(sql, "ะกร๗","12");
        }

        @Test
        void testGetForList() {
            String sql = "SELECT id,name,grade FROM students";
            List<Student> students = dao.getForList(Student.class, sql);

            System.out.println(students.get(0).getName());
        }

	@Test
	void testGetForValue() {
		String sql = "SELECT name FROM students where id=?";
		String name=dao.getForValue(sql, 1);
		System.out.println(name);
	}
    */
}
